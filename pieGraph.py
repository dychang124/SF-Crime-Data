# graph pie chart indicate each 'Incident Category' and combine those has less than 2.5% as 'Other'
                                                                                                                        import pandas as pd                                                                                                     import matplotlib.pyplot as plt                                                                                                                                                                                                                 filepath = 'criminal.csv'                                                                                               df = pd.read_csv(filepath)                                                                                                                                                                                                                      incident_counts = df['Incident Category'].value_counts(normalize=True) * 100                                                                                                                                                                    # Combine categories with less than 2.5% into 'Other'                                                                   threshold = 2.5
other_categories = incident_counts[incident_counts < threshold].index
df.loc[df['Incident Category'].isin(other_categories), 'Incident Category'] = 'Other'                                                                                                                                                           # Recalculate incident counts after combining categories
incident_counts = df['Incident Category'].value_counts()                                                                                                                                                                                        # Create the pie chart                                                                                                  plt.figure(figsize=(10, 10))
plt.pie(incident_counts, labels=[f'{category} ({count})' for category, count in incident_counts.items()], autopct='%1.1f%%', startangle=90)                                                                                                     plt.title('Incident Category Distribution', fontsize=16)
plt.show()

